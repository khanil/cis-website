{% macro render(item) %}
<div class="catalog-item">
  <div class="img-wrapper">
    <img src="{{item.imgSrc}}" alt="{{item.alt}}">
  </div>
  <h2 class="h-item">{{item.name}}</h2>
  <p>{{item.desc}}</p>
  <a href="./product.html"><button>Подробнее</button></a>
</div>
{% endmacro %}